from bridgelib import *
import math as math
import random as random
from tqdm import tqdm
import copy

b1 = Bridge(1.27, 200, 980, 100, 2, 2, 75, 91.43)

# def __init__(self, paper_thickness, height, length, flange_width, num_flange_layers, num_web_layers, web_dist, dia_dist):

b1.report()

# run generative design on bridge









